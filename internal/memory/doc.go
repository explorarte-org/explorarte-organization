// Package memory implements the bounded organizational-memory domain.
//
// Memory is organizational evidence-backed knowledge, not agent scratch space.
// Agents may propose candidates, but publication is a separate reviewed action.
// Clinical or secret-classified content is rejected before it can become a
// memory candidate, and approved entries never gain authority over canonical
// policy or capabilities.
package memory
