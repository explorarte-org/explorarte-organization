# Unidad transversal: empresa

## Naturaleza
`kind: executive_layer`. `leaderless: True` (ver `organization.yaml`).

## Roles
- `empresa/human`: sin perfil ejecutable generado (sin summary en canonical)
- `empresa/ceo`: Coordinar entre departamentos y escalar a Eduardo.
- `empresa/ceo_observer`: propuesto, pendiente de aprobación del owner (ver decisions-required.yaml)

## Delegación y escalamiento
Unidad sin líder (`leaderless`); cada rol reporta según su propio `reports_to` canonical.

## Fronteras con otros departamentos
Este AGENT no concede autoridad, capacidades ni ruteo de modelo:
esas decisiones viven exclusivamente en `docs/canonical/`.
